spring:
  cloud:
    gateway:
      metrics:
        enabled: true

      
      routes:
      
      - id: currency-conversion
        uri: http://localhost:8082/
        predicates:
        - Path=/currency-conversion/retrieve/**