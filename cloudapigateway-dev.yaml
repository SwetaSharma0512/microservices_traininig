spring:
  cloud:
    gateway:
      metrics:
        enabled: true

      
      routes:    
      - id: conversion
        uri: /currency-conversion/retrieve/**
        predicates:
        - Path=/currency-conversion/retrieve/**