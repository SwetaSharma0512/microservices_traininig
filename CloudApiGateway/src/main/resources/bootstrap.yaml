spring:
  cloud:
    config:
      uri: ${currency-calculator.configserverurl:http://localhost:8888}
      fail-fast: false
      label: 
  
  application:    name: cloudapigateway    
  profiles:    active: dev

server:  port: 8089