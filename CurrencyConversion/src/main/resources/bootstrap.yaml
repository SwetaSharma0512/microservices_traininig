spring:
  cloud:
    config:
      uri: ${currency-conversion.configserverurl:http://localhost:8888}
      fail-fast: false
      label: 
  
  application:    name: currency-conversion
   
  profiles:    active: dev    